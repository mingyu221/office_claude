# 2026-09-17 작업 내역 — 인계문서 이후

`BASE = /mnt/af2results/mingyu/workspace/ppi_discovery` · `TBL = BASE/result/table`

---

## 1부. Track A 판정 (RF2-PPI)

| # | 셀 | 무엇을 / 왜 바꿨나 | 결과 | 산출 파일 |
|---|---|---|---|---|
| 1 | `15b` | CELL 15 는 로그 꼬리만 봐서 tqdm 진행률이 안 잡혔다. `\r`→`\n` 변환 후 파싱 | s/pair 로 GPU 오배치를 분 단위에 감지 | — |
| 2 | `23`·`24` | 3,626쌍 ×3 집계 + 양성대조군 | **대조군 0.266** (기준 0.74), `mean≥0.74` 0개 | `TBL/trackA_RF2PPI_{all_pairs,ranked}.csv` |
| 3 | `24b` | CELL 24 가 "깊이 부족"을 **고정 문자열**로 출력했다. 실제 깊이를 읽게 고침 | 깊이 **1,451** = 스크리닝 75분위(1,392) 위 → 깊이 탓 아님. 대조군 위에 **531/3,625** | — |
| 4 | `24c` | 대조군만 **체인 순서가 반대**였고 절편은 미사용(`SEGMENT_BAITS=[]`). 기존 a3m 을 잘라 8변형 생성 | 최고 **0.251**. 절편은 오히려 **0.039~0.114** 로 악화 | `TBL/trackA_control_variants.csv` |
| 5 | `24d`·`50` | ★ **로직 변경**: 지금까지 전부 단량체×단량체였다. 문헌상 CooC1 Ni 자리는 **이량체 계면**(각 monomer Cys112/114) | **진행 중** | `BASE/result/paired/dimer/in_t*.log` |

---

## 2부. Boltz 눈금 검증

| # | 셀 | 무엇을 / 왜 | 결과 | 파일 |
|---|---|---|---|---|
| 6 | `28a-11` | ipTM 잡음을 몰랐다. 세 균주 **직교체**는 입력이 같으니 차이가 곧 잡음 | `metal_rmsd` Δ **0.015** / `ipTM` Δ **0.102**(최대 0.309) | `TBL/ortholog_reproducibility.csv` |
| 7 | `28a-12`·`13` | Boltz 쪽엔 대조군이 **없었다**. CooC1–ChCODH2 를 5표본으로 | **ipTM 0.300** — 후보 8개가 전부 위 → **2번째 대조군 실패** | `TBL/boltz_replicate_summary.csv` |
| 8 | `28a-7` 수정 | 폴더를 `["out","out_focus"]` 로 박아놔 `out_cmp` 를 안 읽었다 → `out*` 전부 훑기 | 348 → **411 구조**. MG1655·Y19 Ni 배치 확보 | `TBL/boltz_ni_placement.csv` |
| 9 | `28a-8b` | "붙었다/안 붙었다"만 봤다. **도너 종류로 등급** | **A(Cys4) 4** · B 9 · C 1 · D 8. **YeiR 가 D 로 강등**(접촉 1개) | `TBL/ni_site_grade.csv` |

---

## 3부. Folddisco 노선 재구축

| # | 셀 | 무엇을 / 왜 | 결과 | 파일 |
|---|---|---|---|---|
| 10 | `33` | ID 계열이 균주마다 달랐다. **대응표를 둘로 분리** — 모티프↔모티프 / 모티프→프로테옴 | '검출 차이' 와 '유전자 부재' 를 구분 가능 | `search/fdm_motif_vs_*.m8` |
| 11 | `34` | 두 축 교차 판정 | BL21 29 중 **공통 15 / 검출차이 12 / 특이 2** | `TBL/motif_metal_3strain.csv` |
| 12 | `35` | ATP 는 필터가 아니라 표시로 | **23~26%** 보유 → 선별력 없음 확정 | `TBL/motif_atp_3strain.csv` |
| 13 | `36` | Tier 1/2/3, 삭제 없이 우선도만 | 1:24 2:49 3:7 | `TBL/FOLDDISCO_candidates.csv` |
| 14 | `36b` | BL21 만 찍고 PPI 점수를 뺐었다 → **80개 전부 + 모든 열**, 열마다 신뢰 등급 | 거르지 않음 | `TBL/ALL_motif_candidates_full.csv` |
| 15 | `36c` | 검출 차이 4개의 **Cys 보존 검사** | ErpA·PgaC·YeeO **fident 1.000**, Cys 보존 → **전부 구조예측 인공물**(v6 vs v4) | — |

---

## 4부. Folddisco 질의 자체를 의심

| # | 셀 | 무엇을 / 왜 | 결과 | 파일 |
|---|---|---|---|---|
| 16 | `29b` | 컷오프 1.0 Å 가 맞는지 검증한 적 없음 → 1.0/1.5/2.0 | **히트 불변**, 최대 min_rmsd 0.745 → `--rmsd` 는 병목이 아니었다 | `TBL/folddisco_rmsd_sweep.csv` |
| 17 | `29c` | ★ **로직 변경**: 질의를 바꿔야 한다. 실제 Ni 잡은 **Cys4 기하**를 템플릿으로 | 103·56·47 (2~4배 증가). 단 **HypA·HypB·Nik 전부 놓침** 확인 | `TBL/folddisco_cys4_requery.csv` |
| 18 | `29d` | Cys4 × 균주 특이 교차 | **버그** — `classify_MG1655_vs_BL21DE3.csv`(4,300행=MG1655 기준)를 집어 교집합 0. 내용 기반 선택으로 수정 | — |
| 19 | `29e` | 분류표 대신 **mmseqs 로 직접** 물음 | 153 중 specific 7 + low 3 = **10개** | `TBL/cys4_bl21_strain_specificity.csv` |
| 20 | `29f`·`29h`·`29i` | ★ 당신 지적 — CooC1 은 호모다이머. `A112,A114,B112,B114` | `--covered-node 4` 에서 **0**. `node_count` 전부 2 → **AlphaFold DB 가 단량체라 계면 질의 불가**. 노선 폐기 | `folddisco/{dimer,covnode,widen}/` |
| 21 | `29g` | 프로테옴 "없음"은 3가지를 뭉침 → **게놈**(`--search-type 2`)으로 재확인. 이후 pseudogene 기준을 `fident≥0.80` 으로 좁힘 | 게놈에도 없음 **2개** | `TBL/cys4_genome_check.csv` |

---

## 5부. Track C 대조군 → Foldseek 전환

| # | 셀 | 무엇을 / 왜 | 결과 | 파일 |
|---|---|---|---|---|
| 22 | `29j` | ★ 당신 지적 — **Y19 의 CooC 를 못 찾으면 대조군 실패다** | Y19 CooC(`AHZ96930.1`)를 기본·확장 **모두에서 못 찾음** → **3번째 대조군 실패** | — |
| 23 | `29k` | ★ **도구 전환**: 모티프(Folddisco) → **구조 전체(Foldseek)** | **CooC1 → Y19 CooC tm 0.938** ✅ 대조군 통과. BL21 최고는 ApbC 0.690 (CooC 없음) | `result/foldseek/cooc1_vs_*.m8` |
| 24 | `29k`(B) | 1순위 후보의 정체 규명 | **`QJZ12568.1` = HypA 폴드** (tm 0.664/0.680) | `result/foldseek/QJZ12568.1_vs_*.m8` |

---

## 6부. 정리 · 다음

| # | 셀 | 내용 | 파일 |
|---|---|---|---|
| 25 | `39` | ★ 당신 지시 — **필터링 대신 등급제**. 제외 0, `reason` 열에 가감점 | `TBL/FINAL_candidates.csv` |
| 26 | `40`·`41` | 논문 Fig.2 — Y19 는 CooC·CooT·CooJ·HypB 보유. **진짜 대조군 세트** | `TBL/coo_operon_homologs.csv` |
| 27 | `42`(50 STEP5) | HypA 폴드 전수 조사 | `TBL/hypA_fold_sweep.csv` |
| 28 | `50` | 위 전부를 한 셀로. GPU 둘은 `part8_gpu` 로 순차 백그라운드 | `script/part8_gpu.sh` · `log/part8_gpu.log` |

---

## 오늘 로직이 바뀐 지점 (되짚을 순서)

1. **깊이 탓 → 모델 탓** (#3) — 대조군 깊이가 오히려 상위 25% 였다
2. **`--rmsd` → `-d`/`-a`** (#16) — 필터가 아니라 검색 단계가 병목이었다
3. **CooC1 2잔기 → Cys4 4잔기** (#17) — 넓어졌지만 **잔기가 달라 부분집합이 아니었다**
4. **단량체 → 이량체 질의** (#20) — 논리는 맞았으나 **DB 가 단량체**라 불가
5. **프로테옴 → 게놈** (#21) — pseudogene 과 어노테이션 누락을 갈라야 했다
6. **Folddisco → Foldseek** (#23) — 대조군을 통과하는 유일한 도구
7. **필터 → 등급** (#25) — 거른 게 답이면 되돌릴 수 없다
8. **단량체 → 이량체 입력** (#5) — Track A 를 닫지 않고 전략 변경

---

## 7부. Track A 이량체 재실행 — 기준 입력을 이량체+이량체로 확정

### 왜 다시 도는가

CooC1–ChCODH2 대조군은 단량체 입력에서 0.266 이었다. 임계 0.74 는 물론이고
스크리닝 분포에서도 상위 14.73%(534/3,625)에 불과했다. 깊이 1,451 로 상위 25%
였으니 MSA 탓이 아니다. **입력의 형태가 틀렸다.** ChCODH2 는 이량체로 작동하고
CooC1 의 Ni 자리는 Cys112/Cys114 가 두 단량체에서 하나씩 나와 이량체 계면에
만들어진다 — 단량체 CooC1 에는 그 자리가 물리적으로 존재하지 않는다.

### 확정: chain1 = ChCODH2 ×2, chain2 = prey ×2

**기준 입력은 이량체 + 이량체로 고정한다.** 근거는 위의 생물학이다. 찾는 대상이
이량체 샤페론이라면(G3E/SIMIBI 과 — CooC · HypB · UreG · MeaB · COG0523 이
전부 이량체다) prey 복제는 왜곡이 아니라 사전확률이다.

| # | 셀 | 무엇을 | 결과 | 파일 |
|---|---|---|---|---|
| 29 | `53`(B) | 한쪽씩만 2배로 만든 변형 비교 | 아래 표 | `paired/dimer/in_[st]*.log` |
| 30 | `54` STEP 0 | 위상별 기준선·디코이 분포 확보 | both 0.408 / bait 0.438 | `paired/calib/cal_*.log` |
| 31 | `54` STEP 1 | 길이 상한 실측 후 전체 스크린 | 상한 1,916 · 3 replicate | `rf2ppi/in_{both,fall}*_b*.log` |

### 변형별 (RF2-PPI, 3 replicate)

| 변형 | 구성 | mean | sd |
|---|---|---:|---:|
| v0_base | CooC1 254 + ChCODH2 636 | 0.251 | 0.031 |
| t_bait | 254 + 1272 (ChCODH2 만 2배) | 0.425 | 0.010 |
| **t_both** | **508 + 1272 (양쪽 2배) — 기준** | **0.390** | 0.017 |
| t_prey | 508 + 636 (CooC1 만 2배) | 0.261 | 0.040 |

단량체 0.251 에서 이량체 0.390 으로, 대조군이 스크리닝 분포 상위
**14.73% → 2.21%(80/3,625)** 로 올라온다. 입력을 바로잡자 대조군이 배경에서
떨어져 나온다는 것이 이 재실행의 근거다.

### 위상별 기준선 — 하나의 컷오프를 공유하지 않는다

디코이 8개(상호작용하지 않는 단백질)를 두 위상으로 같이 쟀다.

| 위상 | 대조군 | 디코이 mean | 디코이 sd | 간격 | z |
|---|---:|---:|---:|---:|---:|
| both (기준) | 0.408 | 0.316 | 0.033 | 0.093 | 2.77 |
| bait (폴백) | 0.438 | 0.276 | 0.024 | 0.162 | 6.85 |

디코이 평균이 0.276 → 0.316 으로 **같이 오른다**. 위상마다 고유한 오프셋이 있고,
따라서 **두 위상의 점수를 같은 컷오프로 비교하면 안 된다.** 집계 때 각 쌍이 어느
위상으로 채점됐는지(`TBL/trackA_dimer_mode_assign.csv` 의 `mode` 열)를 읽어
해당 위상의 디코이 분포로 z 를 내고, 그 z 로 줄을 세운다.

bait 의 z 가 큰 것은 사실이나, 이는 "공진화를 읽는 모델에서는 단량체 prey 가 더
잘 작동한다"는 도구 특성이지 CooC1 생물학에 대한 반박이 아니다. RF2-PPI 는
구조를 푸는 모델이 아니라 짝지은 MSA 의 공진화를 읽는 모델이고, CooC1 열을
복제해도 새 정보가 들어오지 않은 채 사슬 간 신호의 비중만 희석된다. 원자를
실제로 놓는 Boltz 에서는 이량체+이량체(`CTRL-dimer2x2`)가 옳은 입력이다.

**디코이 표의 한계를 명시한다.** 디코이를 단량체 스크린 하위권에서 골랐으므로
z 의 절대값은 낙관적이다. 두 위상이 같은 디코이 8개를 썼으므로 위상끼리의
비교만 공정하다. n=8 이라 sd 추정도 거칠다. 스크린이 끝나면 전체 분포가
진짜 배경이 되므로 그때 다시 잰다.

### 계산 한계와 그 처리 — 버리지 않고 위상을 낮춘다

pair 표현이 L×L 이라 메모리는 L² 로 간다. 실측 상한은 **L = 1,916**
(1,916 통과 / 2,036 OOM). both 는 L = 1272 + 2×prey 라 prey 가 322aa 를 넘으면
걸리고, 그게 분포의 약 43% 다. bait 는 L = 1272 + prey 라 5.7% 만 걸린다.

그래서 초과분을 버리는 대신 **위상을 낮춰 채점하고 어느 위상이었는지 기록한다.**

| 배정 | 조건 | 처리 |
|---|---|---|
| both | L_both ≤ 1,916 | 기준 위상으로 채점 |
| bait | L_both 초과 · L_bait ≤ 1,916 | 폴백. `mode` 열에 기록 |
| 미채점 | 둘 다 초과 (207쌍, 5.7%) | `TBL/trackA_dimer_oversize.csv` 에 남김 |

미채점 쌍은 단량체 점수와 Folddisco/Foldseek 축으로만 등급을 매기고,
**이량체 점수가 없다는 사실 자체를 표에 남긴다.** 거른 게 답이면 되돌릴 수 없다.

### 부수 발견 — 사슬 순서가 프레임마다 달랐다

대조군 a3m 은 `[CooC1 | ChCODH2]` 로 chain1=CooC1 이고 스크린 `input_file` 은
`[ChCODH2 | prey]` 로 chain1=ChCODH2 다. RF2-PPI 는 두 사슬을 대칭으로 다루지
않는다. 같은 입력을 스크린 프레임으로 다시 재니 0.425 → 0.438 (bait),
0.390 → 0.408 (both). 작지만 0 이 아니므로 기준선은 스크린 프레임 값을 쓴다.

---

## 부록 A. 계산 자원 관련 기술 메모

발표 내용과 무관한 실행 환경 문제. 기록만 남긴다.

- **Boltz-2 는 24 GB 에서 1,526 토큰 이상이 안 들어간다.** 1,272 성공 /
  1,526·1,780 실패. 트렁크(pairformer) 첫 할당에서 6초 만에 죽으므로
  디퓨전 쪽 손잡이(`diffusion_samples`, `max_parallel_samples`)는 영향이 없고,
  `max_msa_seqs` 는 전처리 단계 값이라 실행 중 VRAM 을 거의 못 줄인다.
  메모리 효율 삼각주의 커널(trifast)은 이 빌드에 `--no_trifast` 플래그 자체가
  없어 경로가 닫혀 있다. 남은 손잡이는 토큰 수뿐이다.
  → `CTRL-dimer2x2` 는 이 하드웨어에서 포기하고, 이량체 대조군 판정은
  RF2-PPI 쪽 숫자로 낸다.
- **RF2-PPI 의 L 상한은 1,916** (`max_split_size_mb:512` 기준, 실측).
- **`PYTORCH_CUDA_ALLOC_CONF=expandable_segments` 는 rf2ppi 환경에서 쓰면 안 된다.**
  PyTorch 2.1+ 옵션이고 이 환경은 1.x 라 CUDA 초기화 자체가 실패한다.
  `max_split_size_mb` 는 1.x/2.x 공통이다.
- 실패 판정은 `stderr` 내용으로 한다. `out of memory` 가 있을 때만 OOM 이고,
  그 밖의 오류는 상한을 정하지 않고 멈춘다. (증상만 보고 뭉치면 환경 오류가
  하드웨어 한계로 둔갑한다 — 한 번 겪었다.)
- 버킷당 별도 호출 + `|| echo BUCKET FAILED`, 로그 기반 재개.

---

## 오늘 로직이 바뀐 지점 (7부 추가분)

9. **대조군 실패 → 입력 형태 문제** — 0.266 이 모델의 무능이 아니라 단량체
   입력 탓이었다. 이량체+이량체에서 상위 14.73% → 2.21%
10. **기준 입력은 생물학이 정한다** — CooC1 의 Ni 자리가 이량체 계면에 있고
    G3E 과가 전부 이량체이므로, prey 복제는 왜곡이 아니라 사전확률이다
11. **한 점으로는 못 가른다 → 디코이를 같이 잰다** — 위상마다 오프셋이 있으므로
    하나의 컷오프를 공유할 수 없다. 위상별 디코이 분포로 z 를 낸다
12. **못 넣으면 버리는 게 아니라 낮춘다** — 상한 초과분은 위상을 낮춰 채점하고
    어느 위상이었는지 기록한다. 둘 다 안 되는 5.7% 만 미채점으로 남긴다

---

## 9부. 금속 샤페론 과 스윕 · 모티프 질의 재정비 (9/18)

| # | 셀 | 무엇을 / 왜 | 결과 | 파일 |
|---|---|---|---|---|
| 32 | `51` | Folddisco(자리) × Foldseek(폴드) 교차 | 세 범주로 분류 | `TBL/folddisco_x_foldseek.csv` |
| 33 | `52` | ★ 이름이 아니라 **구조로** G3E/SIMIBI 과를 모은다 | 아래 | `TBL/g3e_family_sweep.csv` |
| 34 | `29c` | `--covered-node 4` 를 붙여 재질의 | part / cov4 비교 | `folddisco/cys4/cys4cov4_*.tsv` |
| 35 | `55` | ★ CooC1 **실측** Ni 자리를 단일 사슬로 합쳐 질의 | — | `TBL/cooc1_merged_query.csv` |

### G3E/SIMIBI 과 스윕 — tm 0.5 는 '과' 가 아니었다

CooC 와 상동인 것을 찾는 접근은 이미 실패했다(BL21 최고 ApbC tm 0.690). 그래서
과 전체를 훑기로 했다. HypB · UreG · CooC · MeaB · YjiA · ApbC 가 같은 과이고
공통 업무가 '금속을 다른 단백질에 넣는 것' 이다. seed 25개로 tm ≥ 0.5 를 모으니
**515개**가 나왔다. 그런데 목록에 CheY, elongation factor Tu, two-component
response regulator 가 열 몇 개씩 들어와 있다. tm 0.5~0.6 · fident 0.1 은
**P-loop / CheY 위상 공유**일 뿐 이 과의 기능이 아니다.

등급을 붙였다. 핵심(tm ≥ 0.90) BL21 9 / Y19 20, 주변 28 / 33, 폴드만 206 / 219.
**핵심만 과로 읽는다.** 버리지는 않고 `tier` 열로 남긴다.

부수 확인: **Y19 의 CooC(`AHZ96930.1`)가 ApbC 를 통해 tm 0.603 으로 잡혔다.**
CooC 가 이 과의 구성원인 것은 맞고, 그 근처에 BL21 단백질이 하나도 없다는 것도
Foldseek 결과와 일치한다.

### BL21 전용 판정은 프로테옴만으로 하면 안 된다

프로테옴 검색만으로 3개가 'BL21 전용' 으로 잡혔다. 게놈(`--search-type 2`)까지
확인하니 갈렸다.

| 후보 | tier | 게놈 확인 | 판정 |
|---|---|---|---|
| `QJZ12568.1` | 핵심 | 게놈에도 없음 | **통과** |
| `QJZ13931.1` BcsQ | 폴드만 | **게놈에 온전 (fid 0.99)** | 어노테이션 누락 |
| `QJZ13378.1` capsule | 폴드만 | 게놈에도 없음 | 과 구성원이 아님 |

ErpA·PgaC·YeeO 를 fident 1.000 인데 '전용' 으로 잡았던 것과 같은 유형이다.
프로테옴 부재는 **진짜 없음 / 어노테이션 누락 / pseudogene** 을 뭉친 말이다.

단, `QJZ12568.1` 은 이 스윕에서 **seed 로 직접 넣은 것**(`self_hit = 1`)이다.
과 소속을 이 셀이 독립적으로 입증한 것은 아니다. 근거는 여전히 앞의 네 축
(Cys4 재질의 · HypA 폴드 · Ni 배위 grade A · 프로테옴·게놈 둘 다 부재)이다.

### 모티프 질의의 두 가지를 바로잡았다

**(1) `--covered-node` 를 안 주고 있었다.** 기본값이 0 이라 4잔기 질의에 2잔기만
맞은 구조가 히트로 잡힌다. Cys4 재질의의 103/56/47 이 진짜 Cys4 자리의 수가
아닐 수 있다는 뜻이다. 같은 질의를 `--covered-node 4` 로 다시 돌려 나란히 본다.
앞으로 후보 목록(`FD_CYS4`)은 완전 일치 쪽을 쓴다.

**(2) 질의 템플릿이 예측 구조였다.** Cys4 재질의는 Boltz 예측 구조에서 뽑은
기하다. 실측은 3kji(CooC1)에 있는데, 거기서는 Cys112/Cys114 가 두 단량체에서
하나씩 나오므로 한 사슬에는 Cys 가 2개뿐이다.

CELL 29f 에서 `A112,A114,B112,B114` 로 물어 0 이 나왔을 때, 나는 "AlphaFold DB 가
단량체라 계면 질의가 원리상 불가능하다"고 적었다. **과한 단정이었다.** folddisco 가
맞추는 것은 잔기 쌍의 기하지 사슬 이름이 아니다. 0 의 이유가 (a) 도구가 사슬 간
쌍을 안 쓰는 것인지 (b) 그 기하를 가진 단량체가 없는 것인지 구분하지 않았다.

CELL 55 가 가른다. B 사슬의 두 잔기를 **좌표는 그대로 두고** A 로 개명(번호 +1000)해
단일 사슬 템플릿을 만들면, 질의는 "한 사슬 안의 네 잔기"가 되고 기하는 실측 그대로다.
히트가 나오면 (a) 였고 그 목록이 곧 후보다. 그래도 0 이면 (b) 이고 확실한 음성이다.

**이것이 가설과 정확히 겹친다.** BL21 의 삽입자가 CooC 와 비상동이라면 이량체일
필요가 없다 — 한 사슬 안에서 CooC1 의 이량체 Ni 자리를 흉내 내면 된다. CELL 55 는
바로 그런 단백질이 있는지를 묻는다.

---

## 오늘 로직이 바뀐 지점 (9부 추가분)

13. **"과" 는 tm 0.5 로 정의되지 않는다** — P-loop 위상만 공유하는 것들이 통째로
    들어온다. 등급으로 나누고 핵심만 과로 읽는다
14. **부재는 세 가지를 뭉친 말이다** — 프로테옴에 없음 ≠ 게놈에 없음.
    BcsQ 가 fident 0.99 로 게놈에 온전했다
15. **자기 자신을 근거로 세지 않는다** — seed 로 넣은 것이 스윕에서 나왔다고
    그 스윕이 입증한 것은 아니다 (`self_hit`)
16. **질의 기본값을 확인한다** — `--covered-node` 0, `--rmsd` no limit. 둘 다
    "당연히 이럴 것" 이라 여기고 넘어갔다가 숫자의 의미가 달라졌다
17. **막혔을 때 도구 탓인지 생물학 탓인지 가른다** — 29f 의 0 을 "원리상 불가능"
    으로 닫았는데, 사슬 이름을 없애 보면 갈린다
